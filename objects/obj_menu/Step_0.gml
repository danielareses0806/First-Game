// Se o jogador apertar a tecla Espaço
if (keyboard_check_pressed(vk_space)) {
    // Vai direto para a primeira fase do jogo (mude para o nome exato da sua Room1 se for diferente)
    room_goto(Room1); 
}